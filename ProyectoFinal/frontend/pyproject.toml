[project]
name = "chaucherapp-frontend"
version = "1.0.0"
description = "Frontend de Gradio para priorización de tickets de ChaucherApp"
requires-python = ">=3.11,<3.13"
dependencies = [
    "gradio>=4.44",
    "requests>=2.32",
]

[tool.uv]
package = false
